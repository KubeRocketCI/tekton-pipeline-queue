# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore everything by default and re-include only the prebuilt binaries the
# Dockerfile copies (built by `make build` into dist/).
**
!dist/manager-*
