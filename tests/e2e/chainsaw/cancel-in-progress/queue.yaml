apiVersion: edp.epam.com/v1alpha1
kind: PipelineRunQueue
metadata:
  name: cancel-in-progress-queue
spec:
  selector:
    matchLabels:
      test: cancel-in-progress
  queueKey:
    - lane
  concurrency: 1
  strategy: CancelInProgress
